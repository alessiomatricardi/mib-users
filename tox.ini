[metadata]
license_file = LICENSE

[bdist_wheel]
universal = True

[pytest]
testpaths =
    tests
log_cli = True
addopts =
    --cov=mib
    -r a
    -v
    --cov-report=xml

[coverage:run]
source =
    mib
omit =

