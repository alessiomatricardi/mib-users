[metadata]
license_file = LICENSE

[bdist_wheel]
universal = True

[pytest]
testpaths =
    tests
log_cli = True
addopts =
    --cov=mib
    -r a
    -v
deps =
    -r requirements.dev.txt
commands = 
    pytest --cov-report

[coverage:run]
source =
    mib
omit =

